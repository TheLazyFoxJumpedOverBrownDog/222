<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>AI Blocks Demo</title>
    <script src="https://aframe.io/releases/1.5.0/aframe.min.js"></script>
  </head>
  <body>
    <button onclick="addBlock()" style="position:fixed;top:10px;left:10px;z-index:999;">
      नया ब्लॉक जोड़ो
    </button>

    <a-scene>
      <a-entity position="0 1.6 3">
        <a-camera></a-camera>
      </a-entity>

      <!-- Ground -->
      <a-plane position="0 0 -4" rotation="-90 0 0" width="20" height="20" color="#7BC8A4"></a-plane>
    </a-scene>

    <script>
      let count = 0;
      function addBlock() {
        count++;
        const scene = document.querySelector("a-scene");
        const box = document.createElement("a-box");
        box.setAttribute("position", `${Math.random()*4-2} 1.5 ${-Math.random()*5}`);
        box.setAttribute("color", `hsl(${Math.random()*360}, 70%, 50%)`);
        box.setAttribute("depth", "1");
        box.setAttribute("height", "1");
        box.setAttribute("width", "1");
        box.setAttribute("shadow", "");
        scene.appendChild(box);
      }
    </script>
  </body>
</html>
